#include<bits/stdc++.h>
using namespace std;
void largestWordInSentance(char arr[],int n)
{
  int mx=0;
  int curr=0;
  int i=0;
  while(1)
  {
      
  }

  
}
int main()
{
    int n;
    cin>>n;
    cin.ignore();
    char arr[n+1];
    cin.getline(arr,n);
    cin.ignore();

    return 0;


}